spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp
etsy.key=416rrapzpqu8fuyoqaffpyxn

# Database Connection
#  Server URL. Remember to create the schema (...3306/SCHEMANAME?...)!
spring.datasource.url=jdbc:mysql://localhost:3306/test?useSSL=false&serverTimezone=America/Detroit
spring.datasource.username=root
spring.datasource.password=java2019

# This optional property indicates whether hibernate automatically creates database tables for us
# Set it to create or update, start the app, then set it back to none.
# Update mode is only unsafe if you change your POJO definition? Update uses IF NOT EXISTS when trying to create tables.
spring.jpa.hibernate.ddl-auto=update
# If using ddl-auto, be sure to specify this dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# Allows us to see the statements being prepared/run by Hibernate.
spring.jpa.show-sql=true
